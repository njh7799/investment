# Repository Instructions

이 저장소는 과거 시장 데이터 관리, 지표 분석 및 전략 백테스트를 위한 프로젝트다. 공통 분석 규칙은 이 문서, 전략별 규칙은 `docs/strategies/`를 기준으로 하며 재현성과 미래정보 편향 방지를 우선한다.

## Market data

- `assets/`의 시장 CSV를 직접 편집하지 않는다. 데이터 변경은 `scripts/update_market_data.py`를 통해 재생성한다.
- CSV 스키마, 날짜 형식과 기존 데이터 검증 조건을 유지한다.
- 액면분할과 배당이 반영된 조정 OHLC를 사용하고 원본 가격을 절삭하지 않는다.
- TQQQ 최초 실제 데이터 이전만 QQQ 기반 3배 합성을 허용한다. 실제 데이터가 시작된 이후의 누락일을 합성해서 채우지 말고 오류로 처리한다.
- 전체 재생성, 최신 거래일 검증, 검증 완료 후 파일 교체, 변경이 있을 때만 커밋하는 동작을 보존한다.

## Toss Securities Open API

- 토스증권 연동은 `docs/integrations/toss-api.md`를 따른다.
- 실제 자격증명, access token과 전체 계좌번호를 코드, 문서, 로그, 테스트 fixture나 커밋에 남기지 않는다.
- 사용자가 저장을 요청하지 않은 실시간 계좌 응답은 파일로 기록하지 않고, 계좌번호는 끝 네 자리만 남기고 마스킹한다.

## Analysis conventions

- 별도 전략을 지정하지 않은 백테스트는 `docs/strategies/volatility-allocation/`의 VO(변동성 배분) 기본 규칙을 따른다. 다른 전략군은 `docs/README.md`에서 상태와 문서를 확인한다.

### Backtest execution and valuation

- 신호는 해당 거래일 종가까지 공개된 데이터만 사용해 계산한다.
- 주문은 신호가 확정된 다음 거래일 시가에 체결한다.
- 데이터 마지막 날 신호처럼 다음 거래일 시가가 없으면 미체결로 남긴다.
- 포트폴리오 가치는 각 거래일 종가로 평가한다.
- 나중에 확정된 정보를 과거 거래에 소급하는 등 미래정보 편향을 허용하지 않는다.
- 매매 수수료는 별도 전략 규칙이 없으면 매수와 매도 각각 체결금액의 0.1%로 계산한다. 슬리피지와 세금은 미지정 시 0으로 계산하고 배당 처리 방식은 전략별로 명시한다.
- 내부 계산과 원본 데이터의 정밀도를 유지한다.
- 분석 결과의 금액은 반올림하지 않고 소수점 둘째 자리까지 절삭한다. 이 표시 규칙은 원본 CSV에 적용하지 않는다.

## Strategy research

### Research separation

- 공개 전략 탐색은 현재 기본 전략, 기존 연구 결과와 폐기 목록을 보지 않는 블라인드 조사 단계로 시작한다. 조사자는 투자 목적, 재현성 조건과 허용 데이터만 받고 기존 모델명·성과·판정은 받지 않는다.
- 가능하면 컨텍스트를 상속하지 않은 조사 subagent와 별도의 원전 검증 subagent를 사용한다. 두 역할은 백테스트 결과를 보기 전에 후보 규칙, 기본 파라미터, 출처와 구현상 대체를 확정한다.
- 후보는 `docs/research/preregistrations/`에 먼저 등록한다. 등록 후 결과를 보고 규칙이나 파라미터를 바꾸지 않으며, 변경안은 별도 후속 연구로 등록한다.
- 백테스트가 끝난 뒤에만 기존 `model-census.md`, 배치와 폐기 기록을 비교한다. 실질적으로 같은 규칙은 기존 기록을 갱신하고 신규 규칙만 새 배치에 추가한다.
- 세부 절차는 `docs/research/discovery-protocol.md`, 판정과 승격 조건은 `docs/research/promotion-criteria.md`를 따른다.

### Research direction

- **독립 탐색:** 공개 전략은 VO와의 유사성이나 우열을 탐색 조건으로 삼지 않고, 전략이 주장하는 목적과 적합한 비교군으로 먼저 평가한다.
- **역할 분류:** 독립 평가가 끝난 모델만 VO 대체, VO addon, 별도 실행 전략 또는 연구 기록으로 분류한다.
- **장기 수익:** TQQQ의 높은 장기 수익률을 최대한 활용한다. 이 목표는 VO 대체·addon 판정에 적용하며 공개 전략의 블라인드 탐색 범위를 제한하지 않는다.
- **최대 손실:** MDD는 낮을수록 좋으며, 더 큰 MDD는 여러 기간에서 재현되는 뚜렷한 수익률 개선이 있을 때만 허용한다.
- **회복 속도:** Drawdown duration, Recovery period와 Time underwater는 짧을수록 좋다.
- **일관성:** 고정·롤링 기간 전반에서 재현되는 성과를 우선한다. 모든 기간의 CAGR을 같게 맞추지는 않지만, 한 기간의 높은 수익으로 다른 기간의 부진을 상쇄하지 않는다.
- **간결성:** 성과가 비슷하면 규칙과 파라미터가 더 단순한 전략을 우선하고, 특정 임계값에서만 우수한 모델은 채택하지 않는다.
- **거래 효율:** 실전 후보는 연평균 리밸런싱 20회 이내를 목표로 하며, 초과하려면 비용 반영 후에도 명확한 우위가 있어야 한다.

### Evaluation

- 백테스트 평가 기준과 지표 정의는 `docs/research/methodology.md`를 따른다.
- VO·TQQQ·QQQ 비교는 독립 평가를 대신하지 않으며 통합 단계에서만 사용한다.
- 채택하지 않은 모델은 재조사를 피하도록 `docs/research/rejected-strategies.md`에 규칙, 결과, 사유와 재검토 조건을 남긴다.

## Strategy documents

- 각 전략 디렉터리의 `README.md`만으로 추가 설정 없는 기본 전략을 실행할 수 있어야 한다.
- 기본 규칙을 대체하는 설정은 `variants/`, 기본 전략 위에 조합하는 선택 규칙은 `addons/`에 둔다.
- variant와 addon 문서에는 기본 README와 다른 부분만 기록하고, 경로를 옮기거나 문서를 통합하면 모든 내부 참조를 함께 갱신한다.

## Chart conventions

- 모든 가격 및 백테스트 차트는 생성 전에 `docs/reference/chart-conventions.md`를 확인하고 그 사양을 따른다.

## Verification

- 변경 후 `python -m pytest --quiet`를 실행한다.
- 데이터 로직에는 스키마, 날짜, 결측치 및 OHLC 불변식 검증을 유지한다.
- 공통 분석 로직에는 준비 기간, 임계값 경계, 상태 변경과 다음 거래일 체결 테스트를 포함한다. 전략 전용 검증 조건은 해당 전략 문서를 따른다.
