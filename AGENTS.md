# Repository Instructions

이 저장소는 과거 시장 데이터 관리, 지표 분석 및 전략 백테스트를 위한 프로젝트다. 공통 분석 규칙은 이 문서, 전략별 규칙은 `docs/strategies/`를 기준으로 하며 재현성과 미래정보 편향 방지를 우선한다.

## Market data

- `assets/IXIC.csv`, `assets/QQQ.csv`, `assets/TQQQ.csv`를 직접 편집하지 않는다. 데이터 변경은 `scripts/update_market_data.py`를 통해 재생성한다.
- CSV 스키마, 날짜 형식과 기존 데이터 검증 조건을 유지한다.
- 액면분할과 배당이 반영된 조정 OHLC를 사용하고 원본 가격을 절삭하지 않는다.
- TQQQ 최초 실제 데이터 이전만 QQQ 기반 3배 합성을 허용한다. 실제 데이터가 시작된 이후의 누락일을 합성해서 채우지 말고 오류로 처리한다.
- 전체 재생성, 최신 거래일 검증, 검증 완료 후 파일 교체, 변경이 있을 때만 커밋하는 동작을 보존한다.

## Toss Securities Open API

- 토스증권 API 동작과 스키마는 [공식 문서](https://developers.tossinvest.com/docs)와 [공식 OpenAPI 명세](https://openapi.tossinvest.com/openapi-docs/latest/openapi.json)를 기준으로 한다.
- Client ID와 Client Secret은 루트 `.env`에서만 읽는다. 실제 자격증명, access token과 전체 계좌번호를 코드, 문서, 로그, 테스트 fixture나 커밋에 남기지 않는다.
- 인증은 OAuth 2.0 Client Credentials Grant를 사용하며 계좌 목록은 `GET /api/v1/accounts`로 조회한다.
- 계좌별 API에는 계좌 목록 응답의 `accountSeq`를 `X-Tossinvest-Account` 헤더로 전달한다. `accountNo`를 헤더 값으로 사용하지 않는다.
- 보유주식은 `GET /api/v1/holdings`, 원화·달러 현금 매수 가능 금액은 `GET /api/v1/buying-power`로 조회한다. `cashBuyingPower`를 실제 예수금과 같은 값으로 표현하지 않는다.
- 사용자가 저장을 요청하지 않은 실시간 계좌 응답은 파일로 기록하지 않고 필요한 값만 응답한다. 계좌번호는 기본적으로 끝 네 자리만 남기고 마스킹한다.

## Analysis conventions

- 별도 전략을 지정하지 않은 백테스트는 `docs/strategies/volatility-allocation/`의 VO(변동성 배분) 기본 규칙을 따른다. 다른 전략군은 `docs/README.md`에서 상태와 문서를 확인한다.
- 전략이 다른 계산법을 명시하지 않으면 아래 ATR, ZigZag, 체결 및 평가 규칙을 사용한다. 다른 규칙을 쓰면 차이를 코드와 결과에 명시한다.

### ATR(14)

ATR은 조정 High, Low, Close로 계산한다.

```text
True Range = max(
  High - Low,
  abs(High - Previous Close),
  abs(Low - Previous Close)
)

ATR(14) = 최근 14개 True Range의 단순이동평균
```

- 첫 행의 True Range는 `High - Low`를 사용한다.
- 14개의 True Range가 쌓이기 전에는 ATR 기반 신호를 생성하지 않는다.
- Wilder 평활 방식이 아닌 단순이동평균을 사용한다.

### ZigZag peaks and troughs

극값은 종가 기준 ZigZag로 판정하며 반전 폭이 다음 조건을 모두 충족해야 한다.

- 반전 폭이 반전 확인일의 `ATR(14) × 4` 이상
- 극값 후보 종가 대비 반대 방향으로 10% 이상이며 정확히 10%인 경우도 포함

상승 구간에서는 가장 높은 종가를 고점 후보로 갱신하고 두 반전 조건을 충족하는 하락이 발생하면 해당 후보를 고점으로 확정한다. 하락 구간에서는 가장 낮은 종가를 저점 후보로 갱신하고 두 조건을 충족하는 상승이 발생하면 해당 후보를 저점으로 확정한다.

- 극값 발생일, 극값 확정일과 거래 체결일을 구분한다.
- `전고점`과 `전저점`은 가장 최근에 확정된 극값만 뜻한다.
- 마지막 미확정 후보를 전고점이나 전저점으로 사용하지 않는다.
- 고점 후보 200, 현재 종가 180, 확인일 ATR 5처럼 두 경계값과 정확히 같은 경우에도 고점을 확정한다.

### Backtest execution and valuation

- 신호는 해당 거래일 종가까지 공개된 데이터만 사용해 계산한다.
- 주문은 신호가 확정된 다음 거래일 시가에 체결한다.
- 데이터 마지막 날 신호처럼 다음 거래일 시가가 없으면 미체결로 남긴다.
- 포트폴리오 가치는 각 거래일 종가로 평가한다.
- 극값 발생일로 거래를 소급하는 등 미래정보 편향을 허용하지 않는다.
- 매매 수수료는 별도 전략 규칙이 없으면 매수와 매도 각각 체결금액의 0.1%로 계산한다. 슬리피지와 세금은 미지정 시 0으로 계산하고 배당 처리 방식은 전략별로 명시한다.
- 내부 계산과 원본 데이터의 정밀도를 유지한다.
- 분석 결과의 금액은 반올림하지 않고 소수점 둘째 자리까지 절삭한다. 이 표시 규칙은 원본 CSV에 적용하지 않는다.

## Strategy research

### Research direction

- **장기 수익:** TQQQ의 높은 장기 수익률을 최대한 활용한다.
- **최대 손실:** MDD는 낮을수록 좋으며, 더 큰 MDD는 여러 기간에서 재현되는 뚜렷한 수익률 개선이 있을 때만 허용한다.
- **회복 속도:** Drawdown duration, Recovery period와 Time underwater는 짧을수록 좋다.
- **일관성:** 고정·롤링 기간 전반에서 재현되는 성과를 우선한다. 모든 기간의 CAGR을 같게 맞추지는 않지만, 한 기간의 높은 수익으로 다른 기간의 부진을 상쇄하지 않는다.
- **간결성:** 성과가 비슷하면 규칙과 파라미터가 더 단순한 전략을 우선하고, 특정 임계값에서만 우수한 모델은 채택하지 않는다.
- **거래 효율:** 실전 후보는 연평균 리밸런싱 20회 이내를 목표로 하며, 초과하려면 비용 반영 후에도 명확한 우위가 있어야 한다.

### Evaluation

- 전체 데이터, 각 전략 README에 명시한 고정 평가 기간과 롤링 기간에서 성과를 비교한다.
- 주요 지표는 CAGR, 평균·최악 MDD, 회복 관련 기간, 거래 빈도와 비용 민감도다.
- `Drawdown duration`은 직전 고점부터 저점까지, `Recovery period`는 저점부터 직전 고점 회복까지, `Time underwater`는 직전 고점부터 완전 회복까지로 계산한다.
- 최대 MDD 구간의 회복 관련 기간과 전체 구간의 최장 `Time underwater`를 별도로 보고한다. 데이터 종료일까지 회복하지 못한 구간은 미회복 상태와 종료일 기준 경과 기간을 표시한다.
- 연평균 리밸런싱은 체결일 수를 백테스트 연수로 나누어 계산하고, 같은 날의 리밸런싱은 한 번으로 센다.

## Strategy documents

- 각 전략 디렉터리의 `README.md`만으로 추가 설정 없는 기본 전략을 실행할 수 있어야 한다.
- 기본 규칙을 대체하는 설정은 `variants/`, 기본 전략 위에 조합하는 선택 규칙은 `addons/`에 둔다.
- variant와 addon 문서에는 기본 README와 다른 부분만 기록하고, 경로를 옮기거나 문서를 통합하면 모든 내부 참조를 함께 갱신한다.

## Chart conventions

- 모든 가격 및 백테스트 차트를 만들기 전에 `docs/market-events.md`를 확인한다. 차트 기간에 등록된 주요 급락 사건이 있으면 해당 날짜에 세로 점선을 긋고 X축 가까이에 문서의 사건명을 표시한다.
- 사건 표시는 `#78909C`, 두께 `0.5`, 불투명도 `0.8`의 점선으로 가격선과 모델선 뒤에 둔다. 가까운 라벨은 세로 위치를 엇갈리게 배치하며, 대화형 차트에서는 주요 배경을 툴팁으로 제공한다.
- 문서에 없는 사건 원인을 추측하지 않는다. 사건 설명이 필요하면 당시 자료를 확인해 `docs/market-events.md`를 먼저 갱신한다.
- 가격 차트는 조정 종가를 `#222222`, 두께 `0.5`의 실선으로 표시한다.
- 확정된 고점은 발생일에 `#0066FF`, 저점은 발생일에 `#FF1A1A` 원형 점으로 표시하며 크기는 `s=2.5`로 한다. 마지막 미확정 후보는 표시하지 않는다.
- IXIC가 전 거래일 종가 대비 3% 이상 하락한 날은 `#546e7a`, 두께 `0.25`, 불투명도 `1.0`의 세로 실선으로 표시한다. 이 선은 가격선과 극값 점 뒤에 둔다.
- 기본 그리드는 표시하지 않고 X축은 1년 간격의 `YYYY` 형식을 사용한다.
- 기본 차트 크기는 16×8인치, 출력은 여백을 정리한 180 DPI PNG로 한다.
- 가격 및 백테스트 차트는 전체 거래일 데이터를 그대로 사용한다. 표시 성능이나 파일 크기를 이유로 데이터를 다운샘플링·집계·보간하지 않는다.

### Model comparison charts

- 모델 비교에는 사용자가 별도로 언급하지 않아도 TQQQ 단순 보유와 QQQ 단순 보유를 기본 비교군으로 포함하고 모든 비교군의 시작 자산을 동일하게 맞춘다.
- TQQQ 단순 보유와 QQQ 단순 보유는 같은 청색 계열로 고정한다. TQQQ는 `#2979FF`, QQQ는 `#40C4FF` 실선으로 표시하며 두 기준선의 두께는 `0.5`로 한다.
- 비교 모델은 기준선과 구분되도록 청색 계열을 제외하고, 요청에 적힌 순서대로 `#FF9100`, `#00C853`, `#D500F9`, `#FF1744`, `#AA00FF`, `#FFD600` 색상의 두께 `0.5` 실선을 사용한다.
- 모델 비교 차트는 최신 공통 거래일이 속한 달력연도를 기준으로 다음 기간을 각각 생성한다. 달력연도 구간에 실제로 존재하는 첫 공통 거래일부터 마지막 공통 거래일까지 사용하며 기간 경계는 중복하지 않는다.
  1. 올해 1월 1일부터 최신 공통 거래일까지
  2. 1년 전, 2년 전, 3년 전, 4년 전의 각 달력연도
  3. 5년 전부터 9년 전까지
  4. 10년 전부터 14년 전까지
  5. 15년 전부터 19년 전까지
  6. TQQQ 합성 데이터 시작일부터 20년 전 달력연도 말까지

예를 들어 최신 공통 거래일이 2026년에 있으면 `2026년 YTD`, `2025년`, `2024년`, `2023년`, `2022년`, `2017–2021년`, `2012–2016년`, `2007–2011년`, `TQQQ 합성 시작–2006년` 차트를 생성한다.

## Verification

- 변경 후 `python -m pytest --quiet`를 실행한다.
- 데이터 로직에는 스키마, 날짜, 결측치 및 OHLC 불변식 검증을 유지한다.
- 분석 로직에는 ATR 준비 기간과 임계값 경계, 후보 갱신과 확정, 마지막 미확정 후보, 다음 거래일 체결 테스트를 포함한다.
